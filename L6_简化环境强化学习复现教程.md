# L6 简化环境强化学习复现教程

日期：2026-06-26

本文目标：在不依赖 Gazebo/SITL 的轻量环境中，先建立 AKPF 特征强化学习的最小可训练闭环。Gazebo 仍用于后续部署验证，不用于主训练。

---

## 1. 当前结论

已完成 L6 第一版工程骨架，并补充 L6.1/L6.2/L6.3 的工程入口：

```text
1. 新增 l6_rl_training Python 包；
2. 复用 L3 的 S1-S5 抽象几何；
3. 实现 Gym-style reset/step 环境；
4. 实现 baseline / akpf / full / perception 四种观测；
5. 实现可选 Safety Shield 速度裁剪；
6. 实现自包含 PyTorch PPO 训练入口；
7. 实现策略评估入口；
8. 支持 fixed / random / mixed 场景池；
9. 支持 perception noise、ray dropout、map dropout；
10. 训练和评估 CSV 增加 d_eff、Shield 激活率、cloud_valid、map_age 指标。
```

第一版 L6 是二维定高简化环境，主要验证“AKPF 特征是否更容易学习”。它不是 Gazebo 飞控验证，也不替代 L4/L5 多终端仿真。

当前第一阶段验收结果：使用 AKPF 观测、Safety Shield 和 L3 风格 teacher 行为克隆 warm-start，导出策略 `/tmp/l6_akpf_bc_pass/policy.pt` 后，在 S1-S5 简化环境中每个场景评估 10 次均通过：

```text
S1_single_front_obstacle:  success=1.00, collision=0.00, timeout=0.00, mean_goal_dist=0.4731, mean_min_clearance=0.5688
S2_narrow_gate:           success=1.00, collision=0.00, timeout=0.00, mean_goal_dist=0.4721, mean_min_clearance=0.6588
S3_corridor:              success=1.00, collision=0.00, timeout=0.00, mean_goal_dist=0.4581, mean_min_clearance=1.0706
S4_table_or_low_obstacle: success=1.00, collision=0.00, timeout=0.00, mean_goal_dist=0.4621, mean_min_clearance=1.1873
S5_corner:                success=1.00, collision=0.00, timeout=0.00, mean_goal_dist=0.4510, mean_min_clearance=0.5994
```

当前已经验证的是“AKPF 特征 + L3 teacher warm-start + L5 风格 Shield”的可学习闭环。PPO 微调入口已可运行，但稳定收敛和 baseline 对比还需要更长训练与统计实验。

截至 2026-06-27，已补充 perception 观测 smoke test：

```text
baseline obs_size=21
akpf obs_size=20
full obs_size=41
perception obs_size=29
```

已验证最小 perception 训练与评估命令能产出 `/tmp/l6_perception_smoke/policy.pt`。该 smoke policy 只用于检查代码路径，不代表最终性能。

已验证 L6.1 长训基础设施 smoke：`eval_metrics.csv`、`metrics.csv`、`policy.pt`、`policy_update_0001.pt` 均能正常生成，`--init-policy` 可加载已有 checkpoint 继续训练。该 smoke run 只用于验证训练基础设施，不代表最终策略效果。

---

## 2. 编译

```bash
cd "/mnt/c/Users/admin/Documents/无人机强化学习 2"
source /opt/ros/humble/setup.bash
colcon build --packages-select l6_rl_training --symlink-install
source install/setup.bash
```

期望包含：

```bash
ros2 pkg executables l6_rl_training
```

```text
l6_rl_training l6_eval_policy
l6_rl_training l6_train_ppo
```

---

## 3. 快速冒烟训练

```bash
cd "/mnt/c/Users/admin/Documents/无人机强化学习 2"
source /opt/ros/humble/setup.bash
source install/setup.bash

ros2 run l6_rl_training l6_train_ppo \
  --obs-mode akpf \
  --use-shield \
  --bc-steps 300 \
  --updates 3 \
  --num-envs 5 \
  --rollout-steps 64 \
  --out /tmp/l6_smoke_akpf
```

期望看到每个 update 输出：

```text
update=0001 episodes=... success=... collision=... timeout=... reward=...
saved_policy=/tmp/l6_smoke_akpf/policy.pt
```

perception 观测最小冒烟：

```bash
ros2 run l6_rl_training l6_train_ppo \
  --obs-mode perception \
  --use-shield \
  --scenario-mode mixed \
  --random-scenarios 3 \
  --perception-noise-std 0.02 \
  --ray-dropout-prob 0.05 \
  --map-dropout-prob 0.02 \
  --bc-steps 2 \
  --updates 1 \
  --num-envs 4 \
  --rollout-steps 16 \
  --out /tmp/l6_perception_smoke
```

---

## 4. 第一轮对比训练

AKPF 特征：

```bash
ros2 run l6_rl_training l6_train_ppo \
  --obs-mode akpf \
  --use-shield \
  --bc-steps 2500 \
  --updates 0 \
  --num-envs 10 \
  --rollout-steps 128 \
  --out /tmp/l6_akpf_bc_pass
```

若要继续 PPO 微调，可在 warm-start 基础上增加 update 数：

```bash
ros2 run l6_rl_training l6_train_ppo \
  --obs-mode akpf \
  --use-shield \
  --bc-steps 2500 \
  --updates 80 \
  --num-envs 10 \
  --rollout-steps 128 \
  --out /tmp/l6_akpf_shield
```

baseline 距离采样：

```bash
ros2 run l6_rl_training l6_train_ppo \
  --obs-mode baseline \
  --use-shield \
  --updates 80 \
  --num-envs 10 \
  --rollout-steps 128 \
  --out /tmp/l6_baseline_shield
```

full 观测：

```bash
ros2 run l6_rl_training l6_train_ppo \
  --obs-mode full \
  --use-shield \
  --updates 80 \
  --num-envs 10 \
  --rollout-steps 128 \
  --out /tmp/l6_full_shield
```

每次训练会输出：

```text
policy.pt
policy_best.pt
best_eval.csv
metrics.csv
eval_metrics.csv
policy_update_XXXX.pt
```

### L6.1：PPO 长训与 baseline 对比

建议至少跑以下四组，先固定 S1-S5：

```bash
ros2 run l6_rl_training l6_train_ppo \
  --obs-mode baseline \
  --use-shield \
  --scenario-mode fixed \
  --updates 200 \
  --num-envs 16 \
  --rollout-steps 128 \
  --bc-steps 0 \
  --eval-every 10 \
  --eval-episodes 5 \
  --save-every 25 \
  --out /tmp/l6_l61_baseline_fixed

ros2 run l6_rl_training l6_train_ppo \
  --obs-mode akpf \
  --use-shield \
  --scenario-mode fixed \
  --updates 200 \
  --num-envs 16 \
  --rollout-steps 128 \
  --bc-steps 2500 \
  --eval-every 10 \
  --eval-episodes 5 \
  --save-every 25 \
  --out /tmp/l6_l61_akpf_fixed

ros2 run l6_rl_training l6_train_ppo \
  --obs-mode full \
  --use-shield \
  --scenario-mode fixed \
  --updates 200 \
  --num-envs 16 \
  --rollout-steps 128 \
  --bc-steps 2500 \
  --eval-every 10 \
  --eval-episodes 5 \
  --save-every 25 \
  --out /tmp/l6_l61_full_fixed

ros2 run l6_rl_training l6_train_ppo \
  --obs-mode perception \
  --use-shield \
  --scenario-mode fixed \
  --perception-noise-std 0.02 \
  --ray-dropout-prob 0.05 \
  --map-dropout-prob 0.02 \
  --updates 200 \
  --num-envs 16 \
  --rollout-steps 128 \
  --bc-steps 2500 \
  --eval-every 10 \
  --eval-episodes 5 \
  --save-every 25 \
  --out /tmp/l6_l61_perception_fixed
```

长训中会额外输出：

```text
eval_update=0010 episodes=025 success=...
saved_best_policy=/tmp/l6_l61_akpf_fixed/policy_best.pt update=...
policy_update_0025.pt
```

`metrics.csv` 记录 rollout 训练统计，`eval_metrics.csv` 记录每次定期评估的逐场景统计。`policy_best.pt` 按固定评估结果自动覆盖保存，排序优先级为 success_rate、collision_rate、timeout_rate、mean_goal_dist、mean_min_clearance。若训练中断，可从最近 checkpoint 继续：

```bash
ros2 run l6_rl_training l6_train_ppo \
  --obs-mode akpf \
  --use-shield \
  --scenario-mode fixed \
  --init-policy /tmp/l6_l61_akpf_fixed/policy_update_0100.pt \
  --updates 100 \
  --num-envs 16 \
  --rollout-steps 128 \
  --bc-steps 0 \
  --eval-every 10 \
  --eval-episodes 5 \
  --save-every 25 \
  --out /tmp/l6_l61_akpf_fixed_resume
```

#### 2026-06-27 第一轮 L6.1 固定场景结果

为先得到可比较的正式统计，已在 ld666 中完成一轮 60-update 固定 S1-S5 对比。三组均使用：

```bash
--use-shield
--scenario-mode fixed
--updates 60
--num-envs 12
--rollout-steps 96
--bc-steps 0
--eval-every 10
--eval-episodes 3
--save-every 20
```

结果汇总：

```text
obs_mode   out_dir                    best_eval        final_eval       主要问题
baseline   /tmp/l6_l61_baseline_fixed update50: 5/5    update60: 2/5   S1/S3/S5 超时，best 未保存为 checkpoint
akpf       /tmp/l6_l61_akpf_fixed     update20: 5/5    update60: 4/5   S5 超时，训练后期有回落
full       /tmp/l6_l61_full_fixed     update50: 4/5    update60: 2/5   S5 碰撞，full 真值输入仍不稳定
```

逐场景最终评估：

```text
baseline update60:
  S1 timeout, S2 success, S3 timeout, S4 success, S5 timeout
akpf update60:
  S1 success, S2 success, S3 success, S4 success, S5 timeout
full update60:
  S1 timeout, S2 success, S3 success, S4 timeout, S5 collision
```

当前结论：

```text
1. AKPF 观测比 baseline 更快学到可用解，且最终结果更好；
2. baseline 和 AKPF 都出现中间 checkpoint 优于最终 checkpoint 的现象；
3. full 真值几何输入并没有自动稳定，说明问题不只是观测维度，而是 PPO 配方、探索和 checkpoint 选择也需要改进；
4. L6.1 已补充 policy_best.pt / best_eval.csv，后续长训应以 best checkpoint 进入 L6.2/L7.1，而不是盲用最终 policy.pt。
```

### L6.2：感知式观测建模

`perception` 观测只使用 L4 可提供或可近似提供的低维特征：

```text
goal_dir_x, goal_dir_y, goal_dist
current_vx, current_vy
nearest_distance, d_eff
nearest_normal_x, nearest_normal_y
16-ray local distances
map_age, point_count, cloud_valid, shield_active
```

训练命令：

```bash
ros2 run l6_rl_training l6_train_ppo \
  --obs-mode perception \
  --use-shield \
  --scenario-mode mixed \
  --random-scenarios 32 \
  --perception-noise-std 0.03 \
  --ray-dropout-prob 0.08 \
  --map-dropout-prob 0.03 \
  --updates 300 \
  --num-envs 24 \
  --rollout-steps 128 \
  --bc-steps 3000 \
  --out /tmp/l6_l62_perception_mixed
```

#### 2026-06-27 L6.2 fixed perception 结果

已在 ld666 中完成一轮 fixed S1-S5 perception 训练：

```bash
ros2 run l6_rl_training l6_train_ppo \
  --obs-mode perception \
  --use-shield \
  --scenario-mode fixed \
  --perception-noise-std 0.02 \
  --ray-dropout-prob 0.05 \
  --map-dropout-prob 0.02 \
  --updates 60 \
  --num-envs 12 \
  --rollout-steps 96 \
  --bc-steps 800 \
  --eval-every 10 \
  --eval-episodes 3 \
  --save-every 20 \
  --out /tmp/l6_l62_perception_fixed
```

评估汇总：

```text
checkpoint                                  success collision timeout  说明
/tmp/l6_l62_perception_fixed/policy_best.pt 5/5     0/5       0/5     BC 后 update0，固定场景全通过
/tmp/l6_l62_perception_fixed/policy.pt      2/5     1/5       2/5     PPO 到 update60 后退化
```

当前结论：

```text
1. L4 风格 perception 低维观测足以在 fixed S1-S5 中支持可用策略；
2. BC warm-start 得到的策略目前优于后续 PPO fine-tune；
3. 后续进入 L7.1/Gazebo 时应优先使用 policy_best.pt，而不是最终 policy.pt；
4. PPO 微调需要单独改进，例如降低学习率、缩短微调、KL/行为克隆正则或 early stop。
```

### L6.3：随机化与未知场景测试

训练时使用 mixed 场景池，测试时单独生成 random 场景：

```bash
ros2 run l6_rl_training l6_eval_policy \
  --policy /tmp/l6_l62_perception_mixed/policy.pt \
  --episodes-per-scenario 20 \
  --scenario-mode fixed

ros2 run l6_rl_training l6_eval_policy \
  --policy /tmp/l6_l62_perception_mixed/policy.pt \
  --episodes-per-scenario 20 \
  --scenario-mode random \
  --random-scenarios 30 \
  --seed 9000
```

#### 2026-06-27 L6.3 random 泛化结果

先评估 L6.2 fixed perception best policy：

```bash
ros2 run l6_rl_training l6_eval_policy \
  --policy /tmp/l6_l62_perception_fixed/policy_best.pt \
  --episodes-per-scenario 10 \
  --scenario-mode fixed \
  > /tmp/l6_l63_perception_best_fixed_eval.csv

ros2 run l6_rl_training l6_eval_policy \
  --policy /tmp/l6_l62_perception_fixed/policy_best.pt \
  --episodes-per-scenario 5 \
  --scenario-mode random \
  --random-scenarios 30 \
  --seed 9000 \
  > /tmp/l6_l63_perception_best_random_eval.csv
```

结果：

```text
fixed S1-S5: 50/50 success, 0 collision, 0 timeout
random 30 x 5: 145/150 success, collision_rate=0.0333, timeout_rate=0.0000
唯一失败集中在 R017_train_random，表现为 collision。
```

随后训练 mixed 随机场景池的 BC-only perception policy。注意这里 `--updates 0` 是有意的：L6.2 已证明当前 PPO 微调会破坏 BC 策略，因此本轮先只检验随机化示范本身。

```bash
ros2 run l6_rl_training l6_train_ppo \
  --obs-mode perception \
  --use-shield \
  --scenario-mode mixed \
  --random-scenarios 48 \
  --perception-noise-std 0.02 \
  --ray-dropout-prob 0.05 \
  --map-dropout-prob 0.02 \
  --updates 0 \
  --num-envs 16 \
  --rollout-steps 96 \
  --bc-steps 1200 \
  --eval-every 1 \
  --eval-episodes 3 \
  --save-every 0 \
  --out /tmp/l6_l63_perception_mixed_bc
```

评估 mixed BC policy：

```bash
ros2 run l6_rl_training l6_eval_policy \
  --policy /tmp/l6_l63_perception_mixed_bc/policy_best.pt \
  --episodes-per-scenario 10 \
  --scenario-mode fixed \
  > /tmp/l6_l63_mixed_bc_fixed_eval.csv

ros2 run l6_rl_training l6_eval_policy \
  --policy /tmp/l6_l63_perception_mixed_bc/policy_best.pt \
  --episodes-per-scenario 5 \
  --scenario-mode random \
  --random-scenarios 30 \
  --seed 9000 \
  > /tmp/l6_l63_mixed_bc_random_eval.csv
```

结果：

```text
fixed S1-S5: 50/50 success, 0 collision, 0 timeout
random 30 x 5: 145/150 success, collision_rate=0.0000, timeout_rate=0.0333
唯一失败仍集中在 R017_train_random，但由 collision 改为 timeout。
```

当前结论：

```text
1. perception 低维局部地图特征在简化环境中已有较强随机布局泛化；
2. mixed BC 相比 fixed BC 没有提高总成功率，但把 R017 的失败从碰撞变为超时，安全性更好；
3. R017 是随机多障碍窄绕行布局，应作为泛化压力测试保留，不能针对它写特殊规则；
4. L7.1 Gazebo perception 部署应优先测试 /tmp/l6_l63_perception_mixed_bc/policy_best.pt，同时保留 /tmp/l6_l62_perception_fixed/policy_best.pt 做对照。
```

---

## 5. 策略评估

```bash
ros2 run l6_rl_training l6_eval_policy \
  --policy /tmp/l6_akpf_shield/policy.pt \
  --episodes-per-scenario 20
```

输出 CSV：

```text
scenario,episodes,success_rate,collision_rate,timeout_rate,mean_goal_dist,mean_min_clearance,mean_d_eff,shield_active_rate,cloud_valid_rate,mean_map_age
```

---

## 6. 可视化演示

生成 S1-S5 轨迹图：

```bash
cd "/mnt/c/Users/admin/Documents/无人机强化学习 2"
source /opt/ros/humble/setup.bash
source install/setup.bash

mkdir -p artifacts
ros2 run l6_rl_training l6_visualize_policy \
  --policy /tmp/l6_akpf_bc_pass/policy.pt \
  --output artifacts/l6_akpf_bc_pass_trajectories.png \
  --seed 1000
```

当前已生成：

```text
artifacts/l6_akpf_bc_pass_trajectories.png
```

图中灰色为房间边界，棕色为障碍物，绿色圆为目标半径，蓝线为策略轨迹。

---

## 7. 当前边界

```text
1. 当前环境是二维定高简化动力学；
2. 当前 PPO 是项目内轻量实现，不依赖 stable-baselines3；
3. 当前训练不使用 Gazebo、SITL、MAVROS；
4. 当前 AKPF 观测包含全局目标、L3 风格局部导航目标、最近障碍距离/法向、d_eff、吸引/斥力/切向特征和 Shield 激活标志；
5. perception 观测模拟 L4 局部地图可提供的低维特征，但仍是简化环境中的近似；
6. S4 低桌场景在二维定高环境中只保留房间边界，低高度约束留到三维版；
7. PPO 长训、baseline 对比和训练曲线统计仍需要正式长跑；
8. perception policy 需要进入 L7.1 做 Gazebo 部署验证，不能只看 L6 smoke。
```
